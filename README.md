# cyber-sherlock-shared-lib
Cyber Sherlock shared library (FE + BE)

## Available Scripts

In the project directory, you can run:

### `npm run format`

it prettiers the codebase

Then:

### `npm run build`

Builds the app for production to the `dist` folder.

